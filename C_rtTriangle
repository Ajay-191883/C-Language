#include<stdio.h>
int main()
{
    int i,j,k;
    printf("How much no. of rows u wants in your rt angled triangle = ");
    scanf("%d",&k);
    for(i=1;i<=k;i++)
    {printf("\t");
        for(j=1;j<=i;j++)
        {printf("*");
    }
    printf("\n");
    }
}
